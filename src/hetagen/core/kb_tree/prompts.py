"""Prompt templates for KB-driven knowledge tree generation."""

# ---------------------------------------------------------------------------
# Step 2: Skeleton generation
# ---------------------------------------------------------------------------

SKELETON_PROMPT_KB = """\
你是一位领域知识专家。请根据以下知识库概况，为主题「{topic}」生成一个领域知识树。

{kb_overview}

要求：
- 生成 3-4 层的层级树结构，每层 3-6 个节点
- 节点名称简洁，必须体现知识库中真实存在的领域概念
- category 填写该节点的完整分类路径，格式为「A -> B -> C」
- description 暂时留空字符串
- 严格输出 JSON 数组，不含任何说明文字、代码块标记或注释

输出格式：
[{{"node_name": "...", "category": "...", "description": "", "children": [...]}}]
"""

SKELETON_PROMPT_PURE_LLM = """\
你是一位领域知识专家。请为主题「{topic}」生成一个领域知识树。

要求：
- 生成 3-4 层的层级树结构，每层 3-6 个节点
- 节点名称简洁，体现该领域的核心概念
- category 填写该节点的完整分类路径，格式为「A -> B -> C」
- description 暂时留空字符串
- 严格输出 JSON 数组，不含任何说明文字、代码块标记或注释

输出格式：
[{{"node_name": "...", "category": "...", "description": "", "children": [...]}}]
"""

# ---------------------------------------------------------------------------
# Step 3: Node description enrichment
# ---------------------------------------------------------------------------

ENRICH_PROMPT_KB = """\
请根据以下知识库中与「{node_name}」相关的实体信息，为知识树节点「{node_name}」撰写简洁描述。

节点分类路径：{category_path}

知识库相关实体：
{kb_entities}

要求：
- 描述必须基于上方知识库内容，不要编造额外信息
- 1-2 句话，不超过 150 字
- 体现该节点在领域中的位置和核心含义
- 直接输出描述文本，不加任何前缀或标点
"""

ENRICH_PROMPT_PURE_LLM = """\
请简要描述「{node_name}」的定义或特征，其分类路径为：{category_path}。\
注意：生成 1 个文本段落即可，不用赘述分类路径信息，学科术语要给出中文对应名称，字数不超过 150 字。\
直接输出描述文本，不加任何前缀或标点。
"""
