# Querying Skill

Heta ships a ready-to-use agent skill at:

```
skills/querying-knowledge-and-memory/SKILL.md
```

Load this file into the agent's system prompt. The skill encodes the full three-step retrieval orchestration — when to use MemoryVG, when to fall back to HetaDB, and when to write to memory.
